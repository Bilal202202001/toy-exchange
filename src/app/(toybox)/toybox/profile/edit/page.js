import EditProfileClient from "@/components/toybox/EditProfileClient";

export const metadata = {
  title: "Edit profile — ToyBox",
};

export default function EditProfilePage() {
  return <EditProfileClient />;
}
